# Backend API (GabAI chat goes through this server → Ollama)
# VITE_API_URL=http://localhost:3001

# Legacy: no longer used by GabAI (chat uses Ollama via server). Safe to remove.
# VITE_GROQ_API_KEY=
